import { RevealTextDirective } from './reveal-text.directive';

describe('RevealTextDirective', () => {
  it('should create an instance', () => {
    const directive = new RevealTextDirective();
    expect(directive).toBeTruthy();
  });
});
