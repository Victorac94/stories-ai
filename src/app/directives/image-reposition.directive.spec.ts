import { ImageRepositionDirective } from './image-reposition.directive';

describe('ImageRepositionDirective', () => {
  it('should create an instance', () => {
    const directive = new ImageRepositionDirective();
    expect(directive).toBeTruthy();
  });
});
